#!/usr/bin/env bash
set -euo pipefail

# ── Prod Preamble ─────────────────────────────────────────────────────────────
# Shared body is appended below by CI from setup.sh (between __SHARED_BODY__
# markers). Only prod-specific config belongs here.
VBR_MODE="prod"
: "${VBR_RELEASE_SETUP_URL:=__DEFAULT_SETUP_SCRIPT_URL__}"
: "${DIND_IMAGE_REF:=__DEFAULT_DIND_IMAGE_REF__}"
: "${DIND_CONTAINER_NAME:=vibeboyrunner}"
